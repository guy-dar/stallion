language: node_js
node_js:
  - node
cache:
  directories:
    - node_modules
install:
  - apt-get update
  - apt-get install -yq firefox
  - rm -rf /var/lib/apt/lists/*
  - npm install -g npm@latest
  - npm install -g gulp-cli
  - npm install
